<mapper namespace="com.suppleit.backend.mapper.MemberMapper">

    <!-- ✅ ResultMap: 컬럼과 객체 필드 매핑 -->
    <resultMap id="memberResultMap" type="com.suppleit.backend.model.Member">
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="gender" column="gender"/>
        <result property="birth" column="birth"/>
        <result property="memberRole" column="member_role" javaType="com.suppleit.backend.constants.MemberRole"
            typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
    </resultMap>

    <!-- ✅ 회원가입 -->
    <insert id="insertMember" parameterType="com.suppleit.backend.model.Member" useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO Member (email, password, nickname, gender, birth, member_role)
        VALUES (#{email}, #{password}, #{nickname}, #{gender}, #{birth}, #{memberRole});
    </insert>

    <!-- ✅ 이메일로 회원 조회 -->
    <select id="getMemberByEmail" resultMap="memberResultMap">
        SELECT email, password, nickname, gender, birth, member_role
        FROM Member
        WHERE email = #{email};
    </select>

    <!-- ✅ 이메일 중복 검사 -->
    <select id="checkEmail" resultType="int">
        SELECT COUNT(*) FROM Member WHERE email = #{email};
    </select>

    <!-- ✅ 닉네임 중복 검사 -->
    <select id="checkNickname" resultType="int">
        SELECT COUNT(*) FROM Member WHERE nickname = #{nickname};
    </select>

    <!-- ✅ 회원 삭제 -->
    <delete id="deleteMember">
        DELETE FROM Member WHERE email = #{email};
    </delete>

    <!-- ✅ 비밀번호 업데이트 -->
    <update id="updatePassword">
        UPDATE Member 
        SET password = #{password}
        WHERE email = #{email};
    </update>

</mapper>
